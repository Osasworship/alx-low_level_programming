#include <stdio.h>
/**
 * main - Make use of the function printf
 *
 * Return: Zero if there is no error otherwise, return a non-zero value
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

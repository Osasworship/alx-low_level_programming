#include <stdio.h>
/**
 * main - make use of the function puts
 *
 * Return: Zero if there is no errors otherwise, return a non-zero value
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

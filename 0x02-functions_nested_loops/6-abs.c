#include "main.h"
/**
 * _abs - computes the absolute value
 * @n: is the integer of the questiom
 *
 * Return: the abs of the integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
